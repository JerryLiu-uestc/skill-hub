# Skill Hub App

This directory contains the Tauri + React implementation for Skill Hub.

See the repository-level `README.md` for product features, scanning rules, GitHub matching behavior, and development commands.

Common commands:

```bash
npm install
npm run dev
npm run test
npm run lint
npm run build:app
npm run install:local
```
